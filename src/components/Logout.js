import React, { PropTypes } from 'react'
import { Jumbotron} from 'react-bootstrap';

export const Logout = () => {
  return(
    <Jumbotron>
      <p>&nbsp; &nbsp; You are now logged out</p>
    </Jumbotron>
  )
}
